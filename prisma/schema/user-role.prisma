model UserRole {
  usuarioId Int  @map("usuario_id")
  rolId     Int  @map("role_id")
  usuario   User @relation(fields: [usuarioId], references: [id], onDelete: Cascade)
  rol       Role @relation(fields: [rolId], references: [id], onDelete: Restrict)

  @@id([usuarioId, rolId])
  @@map("user_role")
}
