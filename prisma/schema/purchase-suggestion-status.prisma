model PurchaseSuggestionStatus {
  id             Int                  @id @default(autoincrement())
  codigo         String               @unique
  nombre         String
  compraSugerida PurchaseSuggestion[]

  @@map("purchase_suggestion_status")
}
